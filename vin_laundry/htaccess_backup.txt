# Enable Rewrite Engine
RewriteEngine On
RewriteBase /vin_laundry/

# Exclude real files and directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite to index.php
RewriteRule ^(.*)$ index.php?page=$1 [QSA,L]
